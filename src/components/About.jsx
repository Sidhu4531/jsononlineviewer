export default function About() {
  return (
    <section className="about" id="about" aria-label="About JSON Online Viewer">
      <div className="about-inner">
        <h1 className="about-title">JSON Online Viewer — Format, Validate & Explore JSON Instantly</h1>

        <p className="about-lead">
          <strong>JSON Online Viewer</strong> is a free, fast, and privacy-friendly browser tool
          designed to help developers, analysts, students, and API users make sense of JSON data
          in seconds. Whether you are debugging a REST API response, inspecting a configuration
          file, or learning how JSON structures work, our <strong>online JSON viewer</strong>
          gives you an instant, beautifully formatted view of your data — no sign-up, no
          installation, and no limits.
        </p>

        <p>
          The <strong>best JSON viewer</strong> is the one that gets out of your way, and that is
          exactly what we built. Paste your JSON into the editor and the tool immediately
          validates it, highlights the exact line and column of any syntax error, and renders the
          document as a collapsible, interactive tree. You can expand and collapse objects and
          arrays, click any node to see its path, type, and value, and use the built-in search
          to find keys or values deep inside a large document. It is the{' '}
          <strong>json viewer app</strong> that turns raw text into something you can actually
          navigate.
        </p>

        <h2 className="about-h2">Why Choose Our Free Online JSON Viewer?</h2>

        <p>
          Unlike a heavy <strong>JSON viewer Chrome</strong> extension that needs to be installed,
          granted permissions, and updated, <strong>JSON Online Viewer</strong> runs entirely in
          your browser as a progressive web app. It works on Chrome, Edge, Firefox, Safari, and
          any modern mobile browser, making it a true cross-platform{' '}
          <strong>json viewer online</strong> experience. Because all parsing happens locally on
          your device, your data is never sent to a server. That makes it safe for inspecting
          tokens, personal information, or proprietary payloads — something most online tools
          cannot promise.
        </p>

        <p>
          Our <strong>free online JSON viewer</strong> includes every feature you would expect
          from a paid tool. You can format minified JSON into a readable, indented layout with
          one click, or minify a formatted document to save bandwidth. The type filter lets you
          hide arrays, objects, strings, numbers, booleans, or nulls so you can focus on the
          data that matters. The status bar reports the document size in bytes, the total number
          of nodes, the maximum nesting depth, and the line count — useful diagnostics for
          working with very large API responses. You can also load JSON directly from a file
          using drag-and-drop or the file picker, and copy the formatted output back to your
          clipboard in a single click.
        </p>

        <h2 className="about-h2">Built for Developers, Trusted by Teams</h2>

        <p>
          The tool supports light and dark themes, persists your last input locally so you can
          refresh without losing work, and is keyboard-friendly. It is built with React and Vite,
          optimized to load in under a second even on slow connections, and is fully responsive
          on phones and tablets. Validation is strict and standards-compliant, so the{' '}
          <strong>JSON viewer</strong> catches trailing commas, single-quoted keys, unescaped
          characters, and other common mistakes that break parsers downstream.
        </p>

        <p>
          For everyday use, the <strong>free JSON viewer</strong> covers the full workflow: paste
          in a raw API response, format it into something readable, search for a specific field,
          copy just the value you need, and ship. For larger documents, the tree view stays
          performant because only the nodes you expand are rendered — so even a 10&nbsp;MB JSON
          payload remains responsive. There are no ads, no trackers, and no sign-up walls; the
          site exists to make your work easier, not to harvest your data.
        </p>

        <h2 className="about-h2">Use Cases</h2>

        <p>
          Whether you are a backend developer formatting a payload, a frontend engineer
          validating an API contract, a QA tester exploring test fixtures, a data analyst
          inspecting exports, a technical writer preparing documentation samples, or a student
          learning JavaScript object syntax, <strong>JSON Online Viewer</strong> is the{' '}
          <strong>free JSON viewer</strong> you can rely on. Bookmark this page, share it with
          your team, and try it the next time you need to inspect JSON. It is the{' '}
          <strong>best JSON viewer</strong> for everyday use — and it is, and always will be,
          completely free.
        </p>
      </div>
    </section>
  )
}
